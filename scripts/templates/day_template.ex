defmodule DayXX do
  @moduledoc """
  Solution for Advent of Code YYYY - Day XX
  """

  @behaviour AocLib.Solution

  alias AocLib.Types.ProblemInput

  @impl true
  def results do
    [
      :unsolved,
      :unsolved,
      :unsolved,
      :unsolved
    ]
  end

  @impl true
  def solve_part01(input, _is_sample) do
    _lines = ProblemInput.lines(input)

    :unsolved
  end

  @impl true
  def solve_part02(input, _is_sample) do
    _lines = ProblemInput.lines(input)

    :unsolved
  end
end
